import { NextResponse } from "next/server"; import { getCurrentOrganization, requireUser } from "../../../../../lib/supabase/auth"; import { archiveActivity, getActivity, updateActivity } from "../../../../../lib/crm/activities";
export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { const { supabase } = await requireUser(); const org = await getCurrentOrganization(); if (!supabase || !org) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); return NextResponse.json({ activity: await getActivity(supabase, org.id, (await params).id) }); }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const { supabase } = await requireUser(); const org = await getCurrentOrganization(); if (!supabase || !org) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); try { return NextResponse.json({ activity: await updateActivity(supabase, org.id, (await params).id, await request.json()) }); } catch { return NextResponse.json({ error: "NÃ£o foi possÃ­vel atualizar atividade" }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { const { supabase } = await requireUser(); const org = await getCurrentOrganization(); if (!supabase || !org) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); try { return NextResponse.json({ activity: await archiveActivity(supabase, org.id, (await params).id) }); } catch { return NextResponse.json({ error: "NÃ£o foi possÃ­vel arquivar atividade" }, { status: 400 }); } }
