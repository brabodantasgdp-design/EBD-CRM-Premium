import { NextResponse } from "next/server"; import { getCurrentOrganization, requireUser } from "../../../../../../lib/supabase/auth"; import { cancelActivity } from "../../../../../../lib/crm/activities";
export async function POST(_: Request, { params }: { params: Promise<{ id: string }> }) { const { supabase } = await requireUser(); const org = await getCurrentOrganization(); if (!supabase || !org) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); try { return NextResponse.json({ activity: await cancelActivity(supabase, org.id, (await params).id) }); } catch { return NextResponse.json({ error: "NÃ£o foi possÃ­vel cancelar atividade" }, { status: 400 }); } }
