import { NextResponse } from "next/server";
import { archiveProposal, getProposal, updateProposal } from "../../../../../lib/crm/proposals";
import { getCurrentOrganization, requireUser } from "../../../../../lib/supabase/auth";
type Context = { params: Promise<{ id: string }> };
export async function GET(_: Request, context: Context) { const { supabase } = await requireUser(); const organization = await getCurrentOrganization(); if (!supabase || !organization) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); const proposal = await getProposal(supabase, organization.id, (await context.params).id); return proposal ? NextResponse.json({ proposal }) : NextResponse.json({ error: "Proposta não encontrada" }, { status: 404 }); }
export async function PATCH(request: Request, context: Context) { const { supabase } = await requireUser(); const organization = await getCurrentOrganization(); if (!supabase || !organization) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); try { return NextResponse.json({ proposal: await updateProposal(supabase, organization.id, (await context.params).id, await request.json()) }); } catch { return NextResponse.json({ error: "Não foi possível atualizar proposta" }, { status: 400 }); } }
export async function DELETE(_: Request, context: Context) { const { supabase } = await requireUser(); const organization = await getCurrentOrganization(); if (!supabase || !organization) return NextResponse.json({ error: "Acesso negado" }, { status: 403 }); try { return NextResponse.json({ proposal: await archiveProposal(supabase, organization.id, (await context.params).id) }); } catch { return NextResponse.json({ error: "Não foi possível arquivar proposta" }, { status: 400 }); } }
